# Derived/regenerated - not needed in version control, rebuilt automatically
# from resume.md + job_preferences.md by compile_rubric()
data/compatibility_rubric.json

# Derived/regenerated - rebuilt automatically from resume.md + job_preferences.md +
# HOME_ADDRESS by discover_jobs.py's compile_queries()
data/search_queries.json

# Local evaluation data - personal, regenerated by the pipeline, not shared
data/evaluations.db
evals/cases.json

# Python
__pycache__/
*.pyc

# Local-only tool permission overrides (see .claude/settings.json for shared config)
.claude/settings.local.json
